spring:
  application:
    name: Booking-service

  datasource:
    #url: jdbc:mysql://localhost:3306/Booking-service
    url: ${BOOKING_DB_TEST_URL:jdbc:mysql://localhost:3306/Booking-service}
    username: ${BOOKING_DB_USER}
    password: ${BOOKING_DB_PASS}
    CUSTOMER_DB_CLIENT_URL: http://localhost:8081
    REVIEW_DB_CLIENT_URL: http://localhost:8083

#  jpa:
#    hibernate:
#      ddl-auto: update
#    defer-datasource-initialization: true
#
#  sql:
#    init:
#      mode: always

jwt:
  secret: ${JWT_SECRET}

#server:
#  port: 8082